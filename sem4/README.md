### In each folder you can find descrition and solution of problem.

Here will be shortly described themes of tasks.

13. Realise of `PIPE`, analog of `cmd1 | cmd2` in *UNIX*.
14. Realise of *TCP-CLIENT*, *HTTP-SERVER* , *UDP-CLIENT*.
15. Realise of *epoll* read-write socket.
16. Practice with `threads`.
17. Practice with `mutex` and `condvar`.
18. Practice with `semaphore`.
19. Practice with `dll` in *UNIX*. 
20. Realise of `ARP-CLENT` and `UDP-DNS-resolver`.
21. Realise of `HTTP-GET` and `HTTP-POST` query.
22. Realise of `SHA-512` and `AES-256-CBC`encode/decode.
23. Realise of `FUSE` *filesystem*.
24. Realise of Python module `pymatrixdot` and `pycrypto`.

Also realising of `futex`, `mutex`, `condvar`, `semaphore` and the same things you can find
in [my repository](https://github.com/nikita12100/MIPT_TPCC).
